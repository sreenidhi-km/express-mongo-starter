# express-mongo-starter
A Node.js and Express backend providing a RESTful API for user registration. It features a MongoDB integration via Mongoose, environment variable configuration, and custom CORS handling to allow secure communication with a frontend application.
